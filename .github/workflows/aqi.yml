name: AQI Data Collector

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  collect-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r aqi-project/requirements.txt

      - name: Run AQI Collector
        run: python aqi-project/collector.py
